<div class="container chatroom">
  <h3>Chat with#<%= @invite.first_user == current_user ? @invite.second_user.nickname : @invite.first_user.nickname %></h3>

  <div class="messages">
    <% @invite.messages.each do |message| %>
      <div id="message-<%= message.id %>">
        <small>
          <strong><%= message.user.nickname %></strong>
          <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
        </small>
        <p><%= message.content %></p>
      </div>
    <% end %>
  </div>
</div>

<%= render 'messages/form', invite: @invite, user: current_user, band_message: @band_messages %>
